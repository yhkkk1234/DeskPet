# 桌宠项目 - 技术设计文档

> **项目名称**：DeskPet（桌宠）
> **文档版本**：v1.0
> **最后更新**：2026-04-17
> **状态**：设计阶段

---

## 一、项目概述

### 1.1 项目定位

这是一个基于 Ghost 灵魂系统的 AI 桌宠项目，区别于传统 AI 助手强调可靠性、工程性、全能性，本项目追求**情感丰富性**和**不可靠但真实的人类伙伴感**。

### 1.2 核心设计理念

Ghost（灵魂）是本项目的核心概念，通过以下机制实现：
- **人格动态系统**：6维人格属性通过数值工程影响 Prompt，塑造差异化对话风格
- **情感模拟**：感性（爱/恨）+ 理性（回弹力）构成情感动态
- **记忆系统**：长短期记忆 + 重要性评估 + 随机性读取，模拟人类记忆特性
- **主动好奇心**：可选开启的后台截屏认知功能

---

## 二、技术架构

### 2.1 技术栈

| 模块 | 技术选型 |
|------|---------|
| 语言/框架 | C# / .NET 8 / WPF |
| 图形渲染 | SkiaSharp（2D渲染）|
| 数据库 | SQLite + Dapper |
| AI 集成 | Provider 插件化架构 |
| 截图 | Win32 API (PrintScreen) + 透明窗口 |
| 热键 | RegisterHotKey (Win32) |
| 序列化 | System.Text.Json + AES 加密 |
| 语音 (可选) | edge-tts / SAPI |

### 2.2 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                        UI Layer                             │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │ 桌宠渲染  │  │ 对话气泡  │  │ 截图浮层  │  │ 托盘图标  │  │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
├─────────────────────────────────────────────────────────────┤
│                      Ghost Engine                          │
│   ┌────────────┐  ┌────────────┐  ┌──────────────────┐    │
│   │ Soul System │  │ PromptBuilder│  │ Memory System   │    │
│   │ 6维人格     │  │ 动态Prompt   │  │ LTM/STM/印象     │    │
│   │ 感性/理性   │  │ 组装器       │  │ 上下文压缩      │    │
│   └────────────┘  └────────────┘  └──────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│                      Service Layer                          │
│   ┌──────────┐  ┌──────────────┐  ┌──────────────┐         │
│   │ Hotkey   │  │ AI Provider  │  │ Screenshot   │         │
│   │ Service  │  │ (多模型支持)  │  │ Service      │         │
│   └──────────┘  └──────────────┘  └──────────────┘         │
│   ┌──────────┐  ┌──────────────┐  ┌──────────────┐         │
│   │ Activation│  │ TTS Service │  │ ImageGen     │         │
│   │ Service  │  │ (可选)       │  │ (可选)       │         │
│   └──────────┘  └──────────────┘  └──────────────┘         │
├─────────────────────────────────────────────────────────────┤
│                      Data Layer                             │
│        ┌──────────┐  ┌──────────────┐                      │
│        │ .ghost  │  │   SQLite     │                      │
│        │ 文件    │  │   数据库     │                      │
│        └──────────┘  └──────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、Ghost 文件格式

### 3.1 文件结构

Ghost 文件（`.ghost`）是加密的 JSON 数据包，承载灵魂的全部数据。

```json
{
  "version": "1.0",
  "ghostId": "uuid-v4",
  "createdAt": "ISO-8601",
  "activation": {
    "machineFingerprint": "SHA-256-Hash",
    "activatedAt": "ISO-8601",
    "isActivated": true
  },
  "body": {
    "type": "spritesheet",
    "assetPath": "./bodies/default/",
    "defaultExpression": "idle"
  },
  "soul": {
    "innateTendency": { ... },
    "curiosity": { ... },
    "sensibility": { ... },
    "rationality": { ... },
    "memory": { ... },
    "impression": { ... }
  }
}
```

### 3.2 Soul 子系统

Soul（灵魂）由以下组件构成：

| 组件 | 描述 | 数据类型 |
|------|------|---------|
| InnateTendency | 天生倾向性（6维人格）| 6个 0.0~1.0 数值 |
| Curiosity | 好奇心配置 | 枚举 + 数值 |
| Sensibility | 感性（爱/恨）| -100~+100 数值 |
| Rationality | 理性（回弹力）| 非常小的正数值 |
| Memory | 记忆系统引用 | 外键关联 |
| Impression | 主人印象 | 人格评分 + 文字片段 |

---

## 四、核心系统详解

### 4.1 天生倾向性（InnateTendency）

#### 4.1.1 人格属性

6维人格雷达图，生成时一次性随机生成，绝对无法修改：

| 属性 | 范围 | 描述 |
|------|------|------|
| Openness | 0.0~1.0 | 开放性：对新事物的好奇程度 |
| Conscientiousness | 0.0~1.0 | 尽责性：认真程度 |
| Extraversion | 0.0~1.0 | 外向性：主动交流意愿 |
| Agreeableness | 0.0~1.0 | 宜人性：友好程度 |
| Neuroticism | 0.0~1.0 | 神经质：情绪波动程度 |
| Creativity | 0.0~1.0 | 创造性：发散思维程度 |

#### 4.1.2 喜好的主人人格属性

Ghost 有一个"喜好的人格属性"镜像，表示该 Ghost 期望主人拥有的人格特质，用于对主人进行评价打分。

```csharp
public class InnateTendency
{
    // 自我人格属性
    public double Openness;
    public double Conscientiousness;
    public double Extraversion;
    public double Agreeableness;
    public double Neuroticism;
    public double Creativity;

    // 喜好的主人人格属性（权重）
    public double PreferredOpenness;
    public double PreferredConscientiousness;
    public double PreferredExtraversion;
    public double PreferredAgreeableness;
    public double PreferredNeuroticism;
    public double PreferredCreativity;

    public static InnateTendency Generate(SecureRandom rng);
}
```

---

### 4.2 感性系统（Sensibility）

#### 4.2.1 爱恨值

| 属性 | 范围 | 描述 |
|------|------|------|
| LoveHateValue | -100 ~ +100 | 对主人的爱（+）或恨（-）程度 |

#### 4.2.2 情感驱动的对话风格

```csharp
public class DialogueStyle
{
    // 根据 LoveHateValue 调整语气
    public string GetToneInstruction() => LoveHateValue switch
    {
        > 50  => "请用温柔、关怀、体贴的语气与用户交流。适当使用亲昵的表达。",
        > 20  => "请用友好、轻松的语气与用户交流。",
        > -20 => "你保持中立、平和的态度。",
        > -50 => "你对这个用户态度较为冷淡，回应简短。",
        _     => "你对这个用户有些冷淡，保持距离感，语言简洁。"
    };
}
```

---

### 4.3 理性系统（Rationality）

#### 4.3.1 回弹力机制

理性是一种随机动态约束值，将情感拉回中间值的回弹力：

| 属性 | 类型 | 描述 |
|------|------|------|
| SpringConstant | double | 回弹系数，非常小的正值 (0.001~0.01) |
| RandomSeed | double | 随机扰动种子 |

```csharp
// 每 tick 执行一次回弹
public void ApplySpringForce(ref double loveHateValue)
{
    var force = (0 - loveHateValue) * SpringConstant;
    loveHateValue += force;
    
    // 添加随机扰动（模拟真实情感波动）
    loveHateValue += Random.NextDouble() * 0.1 - 0.05;
}
```

**特性**：
- 当 loveHate > 0（爱）→ 回弹力缓慢将其拉向 0
- 当 loveHate < 0（恨）→ 回弹力也会将其拉向 0
- 若突然从爱转向恨，回弹力会减缓这个过程（反之亦然）

---

### 4.4 好奇心系统（Curiosity）

#### 4.4.1 等级设置

| 等级 | 描述 | 默认 |
|------|------|------|
| Off | 完全关闭 | ✅ |
| Normal | 空闲时主动搜索感兴趣的信息 | - |
| Enhanced | 随机后台截屏桌面/软件，记入"主人印象" | - |

#### 4.4.2 实现逻辑

```
空闲时检测
    │
    ▼
根据天生倾向性选择感兴趣的信息类型
    │
    ▼
主动搜索/查找 → 记入短期记忆
    │
    ▼
"Enhanced" 模式下：
    │
    ▼
定时截取用户当前桌面/软件
    │
    ▼
AI 认知分析 → 更新"主人印象"
```

---

### 4.5 主人印象系统（Impression）

#### 4.5.1 人格属性评分

Ghost 对主人的人格属性进行加权累加评分：

```csharp
public class Impression
{
    // 累加的数值评分（用于快速判断）
    public double OpennessScore;
    public double ConscientiousnessScore;
    public double ExtraversionScore;
    public double AgreeablenessScore;
    public double NeuroticismScore;
    public double CreativityScore;

    // 权重来源：InnateTendency.Preferred*
    // 每次获得新印象时，根据事件计算各属性加权值
    // e.g., "主动关心我" → AgreeablenessScore += 5
}
```

#### 4.5.2 文字评价片段

```csharp
public class Impression
{
    // JSON 数组，存储文字版印象片段
    // e.g., ["惯用左手", "经常深夜使用软件", "第一次主动关心我"]
    public List<string> GeneralImpressionSnippets;
}
```

---

### 4.6 记忆系统（Memory）

#### 4.6.1 记忆分类

```
┌─────────────────────────────────────────┐
│              记 忆 系 统                 │
├──────────────────┬──────────────────────┤
│    短期记忆        │      长期记忆         │
│  (ShortTerm)      │   (LongTerm)         │
├──────────────────┼──────────────────────┤
│ - 频繁日常对话     │ - 重要事件            │
│ - 重复性问题      │ - 里程碑时刻          │
│ - 可变的临时信息   │ - 核心人格塑造事件     │
├──────────────────┼──────────────────────┤
│ 快速压缩/劣化     │ 缓慢压缩/劣化         │
│ 随机加载读取      │ 重要事件永不丢失       │
└──────────────────┴──────────────────────┘
```

#### 4.6.2 记忆数据结构

```csharp
public abstract class Memory
{
    public Guid Id;
    public DateTime CreatedAt;
    public DateTime LastAccessedAt;
    public double Importance;     // 0.0 ~ 1.0
    public string Summary;        // 压缩后的摘要
    public bool IsCompressed;
}

public class ShortTermMemory : Memory
{
    public double Accessibility;  // < 0.3 会被"随机加载"决定是否读取
}

public class LongTermMemory : Memory
{
    public bool IsCoreMemory;     // >= 0.8 为核心记忆，永不丢失
}
```

#### 4.6.3 上下文窗口 + 压缩保存

```
对话进行中
    │
    ▼
写入 ContextWindow（内存）
    │
    ▼
Token 达到阈值（如 6000）
    │
    ▼
异步压缩：旧消息 → AI 摘要 → SQLite
    │
    ▼
清空已保存的旧消息，保留最近 3 条连贯性
```

#### 4.6.4 记忆读取规则

1. **越近的记忆越优先读取**
2. **越重要的记忆越优先读取**
3. **不重要的短期记忆随机性加载**（模拟"想不起来"的特性）

---

## 五、动态 Prompt 生成系统

### 5.1 GhostPromptBuilder

核心组件，将 Ghost 的灵魂数据转换为 AI 可理解的 System Prompt。

### 5.2 组装流程

```
┌─────────────────────────────────────────────────────────┐
│                   Prompt 组装流程                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  第一层：人格描述（6维动态生成）                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │ Openness > 0.7  → "你是一个对世界充满好奇的人..."     │    │
│  │ Openness < 0.3  → "你更倾向于稳定和熟悉..."          │    │
│  │ Extraversion > 0.7 → "你健谈且喜欢主动交流..."      │    │
│  │ ...以此类推（共6维）                                │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│                          ▼                              │
│  第二层：情感语气指令（动态叠加）                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │ LoveHate > 50  → "请用温柔关怀的语气回应..."         │    │
│  │ LoveHate < -50 → "请用冷淡简短的语气回应..."         │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│                          ▼                              │
│  第三层：记忆上下文（近期重要记忆片段）                     │
│  ┌─────────────────────────────────────────────────┐    │
│  │ - "根据我们之前的对话..."                           │    │
│  │ - "我记得你提到过..."                              │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│                          ▼                              │
│  最终 Prompt = 基础人设 + 情感语气 + 记忆上下文            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5.3 人格描述生成策略

| 阶段 | 策略 | 说明 |
|------|------|------|
| 原型阶段 | AI 自动生成 | 根据6维数值让大模型生成描述段落 |
| 正式版 | 模板库 | 人工编写描述模板，程序根据数值组合调用 |

**原型阶段生成示例**：

输入：`Openness=0.8, Extraversion=0.2, Neuroticism=0.4, LoveHate=30`

输出：
```
你是一个内心世界丰富但不善言辞的人。你对新事物充满好奇，
思维开放，但性格偏内向，不习惯主动与人交流。你有一定
的情绪波动，但总体能够保持内心的平衡。你对这个用户
有好感，请用温柔而不张扬的语气与对方交流。
```

---

## 六、AI 服务层

### 6.1 Provider 架构

```
┌─────────────────┐
│   IAIProvider   │  ← 统一接口
├─────────────────┤
│ + ChatAsync()   │
│ + VisionAsync() │
│ + DrawAsync()   │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───┴───┐ ┌───┴────┐
│OpenAI │ │DeepSeek│  ← 用户主用
│Provider│ │Provider│
└───┬───┘ └───┬────┘
    │         │
┌───┴───┐ ┌───┴────┐
│ 文心   │ │ 通义    │  ← 后续扩展
│Provider│ │Provider│
└───────┘ └────────┘
```

### 6.2 接口定义

```csharp
public interface IAIProvider
{
    Task<string> ChatAsync(string systemPrompt, List<Message> history);
    Task<string> VisionAsync(string imagePath, string question);
    Task<string> DrawAsync(string prompt);
}
```

### 6.3 支持的模型

| 模型 | 支持情况 | API 兼容性 |
|------|---------|-----------|
| OpenAI GPT-4o / GPT-4o-mini | ✅ 原型支持 | OpenAI 官方 |
| DeepSeek Chat (V3) | ✅ 用户主用 | OpenAI 兼容 |
| 百度文心 | 🔜 后续扩展 | ERNIE Bot API |
| 阿里通义 | 🔜 后续扩展 | OpenAI 兼容 |
| 腾讯混元 | 🔜 后续扩展 | 专用 API |
| 智谱 GLM | 🔜 后续扩展 | GLM API |

---

## 七、数据库设计

### 7.1 表结构

```sql
-- 短期记忆表
CREATE TABLE ShortTermMemories (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    Content TEXT NOT NULL,
    Summary TEXT,
    CreatedAt DATETIME NOT NULL,
    LastAccessedAt DATETIME,
    Importance REAL DEFAULT 0.5,
    Accessibility REAL DEFAULT 0.5,
    IsCompressed INTEGER DEFAULT 0,
    Category TEXT
);

-- 长期记忆表
CREATE TABLE LongTermMemories (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    Summary TEXT NOT NULL,
    Detail TEXT,
    CreatedAt DATETIME NOT NULL,
    LastAccessedAt DATETIME,
    Importance REAL NOT NULL,
    IsCoreMemory INTEGER DEFAULT 0,
    EventType TEXT
);

-- 主人印象表
CREATE TABLE MasterImpressions (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    OpennessScore REAL DEFAULT 0,
    ConscientiousnessScore REAL DEFAULT 0,
    ExtraversionScore REAL DEFAULT 0,
    AgreeablenessScore REAL DEFAULT 0,
    NeuroticismScore REAL DEFAULT 0,
    CreativityScore REAL DEFAULT 0,
    Snippets TEXT,
    UpdatedAt DATETIME NOT NULL
);

-- 会话片段表（用于重建上下文）
CREATE TABLE ConversationChunks (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    StartTime DATETIME NOT NULL,
    EndTime DATETIME,
    Summary TEXT NOT NULL,
    Importance REAL DEFAULT 0.5,
    TokenCount INTEGER
);
```

---

## 八、桌面交互系统

### 8.1 截图流程

```
用户按下快捷键 (默认: Ctrl+Alt+X)
         │
         ▼
┌─────────────────────────┐
│   全局热键监听 (Win32)   │
└─────────────────────────┘
         │
         ▼
┌─────────────────────────┐
│  创建全屏透明截图覆盖层   │
│  显示选取框             │
└─────────────────────────┘
         │
         ▼
┌─────────────────────────┐
│  用户完成选取           │
│  截图保存到临时文件       │
└─────────────────────────┘
         │
         ├──→ AI 识图（调用 Vision API）
         │         │
         │         ▼
         │    生成描述文本
         │
         ▼
┌─────────────────────────┐
│  桌宠跳转到截图区域附近   │
│  显示输入框 + 对话气泡    │
└─────────────────────────┘
```

### 8.2 对话框布局

```
┌────────────────────────────────────────┐
│                                        │
│      ┌──────────────────┐              │
│      │  桌宠气泡 (漫画尾)  │ ← 桌宠的回复   │
│      └──────────────────┘              │
│                                        │
│          🧡 桌宠形象                    │
│                                        │
│  ┌──────────────────┐                  │
│  │    用户输入框     │ ← 截图选框附近    │
│  └──────────────────┘                  │
│                                        │
│  [发送]  [取消]                          │
└────────────────────────────────────────┘
```

---

## 九、激活验证系统

### 9.1 机制说明

```
首次激活流程：
┌─────────────┐
│ Ghost 文件  │
│  包含 ghostId│
└──────┬──────┘
       │ 首次运行
       ▼
┌─────────────────────┐
│ 采集机器指纹:        │
│ CPU ID + 主板序列号   │
│ + 硬盘序列号 (哈希)   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  将 "机器指纹哈希"    │
│  写入 ghost.activation │
│  并标记 isActivated   │
└─────────────────────┘

换机器时：
┌─────────────────────┐
│ 检测到机器指纹不匹配   │──→ 提示用户需要重新激活
│ 拒绝加载 Ghost         │
└─────────────────────┘
```

### 9.2 原型阶段

原型阶段跳过加密/激活验证逻辑，先跑通核心流程。

---

## 十、文件结构

```
F:/桌宠A/
├── DeskPet/                          # 主项目
│   ├── DeskPet.csproj
│   ├── Program.cs
│   │
│   ├── Core/                         # Ghost 核心
│   │   ├── Ghost.cs
│   │   ├── Soul/
│   │   │   ├── InnateTendency.cs     # 6维人格
│   │   │   ├── Sensibility.cs        # 感性
│   │   │   ├── Rationality.cs        # 理性
│   │   │   ├── Curiosity.cs          # 好奇心
│   │   │   └── Soul.cs               # Soul 聚合根
│   │   ├── Memory/
│   │   │   ├── MemoryBase.cs
│   │   │   ├── ShortTermMemory.cs
│   │   │   ├── LongTermMemory.cs
│   │   │   └── Impression.cs
│   │   └── GhostPromptBuilder.cs     # 动态Prompt生成
│   │
│   ├── Services/
│   │   ├── AI/                       # AI 服务
│   │   │   ├── IAIProvider.cs
│   │   │   ├── OpenAIProvider.cs
│   │   │   ├── DeepSeekProvider.cs
│   │   │   └── ProviderManager.cs
│   │   ├── Memory/
│   │   │   ├── ContextWindow.cs
│   │   │   └── MemoryService.cs
│   │   ├── Screenshot/
│   │   │   ├── HotkeyService.cs
│   │   │   └── ScreenshotService.cs
│   │   └── Activation/
│   │       └── ActivationService.cs
│   │
│   ├── Data/                         # 数据层
│   │   ├── GhostFile.cs             # ghost 文件操作
│   │   └── Database.cs              # SQLite 操作
│   │
│   └── UI/                          # UI 层
│       ├── MainWindow.xaml
│       ├── MainWindow.xaml.cs
│       ├── PetRenderer.cs           # 桌宠渲染
│       ├── DialogueBubble.xaml     # 对话气泡
│       └── ScreenshotOverlay.xaml  # 截图浮层
│
├── Bodies/                          # 桌宠躯体资源
│   └── default/
│       ├── idle/                   # 待机动画序列帧
│       ├── happy/                  # 开心动画
│       └── ...其他状态
│
├── Ghosts/                         # Ghost 文件存放
│   └── *.ghost
│
└── DeskPet.sln
```

---

## 十一、开发阶段

### 阶段一：Ghost 核心系统
| 步骤 | 任务 | 产出 |
|------|------|------|
| 1.1 | 创建 C# 项目结构 | 目录划分、csproj |
| 1.2 | 实现 InnateTendency（6维人格）| 随机生成、序列化 |
| 1.3 | 实现 Sensibility（感性/爱恨）| 爱恨值计算 |
| 1.4 | 实现 Rationality（理性/回弹）| 回弹力衰减 |
| 1.5 | 实现 Curiosity（好奇心）| 等级配置 |
| 1.6 | 实现 Impression（主人印象）| 加权评分系统 |
| 1.7 | 实现 GhostPromptBuilder | AI自动生成人格描述 |
| 1.8 | 实现 Ghost 文件加载/保存 | JSON 序列化（原型阶段无加密）|

### 阶段二：记忆系统
| 步骤 | 任务 | 产出 |
|------|------|------|
| 2.1 | 设计 SQLite 表结构 | 4张表 |
| 2.2 | 实现 ContextWindow | 内存中的对话历史 |
| 2.3 | 实现 MemoryService | 重要性评估、摘要生成 |
| 2.4 | 实现压缩保存逻辑 | Token 阈值触发、异步保存 |
| 2.5 | 实现记忆读取逻辑 | 按重要性 + 时间权重读取 |

### 阶段三：AI 服务层
| 步骤 | 任务 | 产出 |
|------|------|------|
| 3.1 | 定义 IAIProvider 接口 | ChatAsync/VisionAsync/DrawAsync |
| 3.2 | 实现 OpenAI Provider | GPT-4o / GPT-4o-mini |
| 3.3 | 实现 DeepSeek Provider | DeepSeek Chat (V3) |

### 阶段四：控制台对话验证
| 步骤 | 任务 | 产出 |
|------|------|------|
| 4.1 | 实现 DialogueService | 对话流程管理 |
| 4.2 | 串联 Ghost + Memory + AI | 端到端对话 |
| 4.3 | 控制台测试界面 | 验证人格差异 |

### 阶段五：桌面交互 UI
| 步骤 | 任务 | 产出 |
|------|------|------|
| 5.1 | WPF 窗口框架 | 无边框、置顶、透明度 |
| 5.2 | Spritesheet 占位渲染 | 色块占位，后期替换 |
| 5.3 | 全局热键注册 | Ctrl+Alt+X |
| 5.4 | 截图覆盖层 | 全屏透明窗口 + 选区 |
| 5.5 | 对话气泡 UI | 漫画尾气泡 |
| 5.6 | 输入框浮层 | 截图选区附近 |
| 5.7 | 桌宠移动逻辑 | 跳转到截图区域 |

### 阶段六：可选功能（后续）
| 功能 | 优先级 | 说明 |
|------|--------|------|
| Ghost 加密/激活验证 | P2 | AES + 机器指纹 |
| TTS 语音 | P2 | edge-tts |
| 生图 API | P3 | Stable Diffusion 等 |
| 好奇心系统 | P3 | 后台截屏，默认关闭 |
| Live2D 迁移 | P3 | 从 Spritesheet 升级 |
| 国内模型扩展 | P3 | 文心/通义/混元/GLM |

---

## 十二、确认决策点

| 决策项 | 最终方案 |
|--------|---------|
| Ghost 载体 | 独立 .ghost 数据文件，灵魂与躯体分离 |
| 人格描述生成 | 原型：AI自动生成 → 正式版：模板库 |
| 数据库 | SQLite + Dapper |
| AI 接口 | 用户自备 API，Provider 插件化 |
| 对话模型 | OpenAI Provider + DeepSeek Provider（用户主用）|
| 桌宠视觉 | 原型：Spritesheet 占位 → 正式版：Live2D |
| 截图交互 | Ctrl+Alt+X 快捷键触发（默认方式）|
| 好奇心后台截屏 | 可选开启，默认关闭 |
| 激活验证 | 原型跳过，验证阶段后实现 |
| 跨平台 | 仅 Windows，暂不考虑 Mac/Linux |

---

## 附录

### A. 参考资料
- 项目灵感来源：Shimeji、Desktop mascot、Live2D
- AI Prompt 工程：OpenAI API 文档、DeepSeek API 文档

### B. 术语表

| 术语 | 定义 |
|------|------|
| Ghost | 灵魂数据文件，包含人格、记忆、情感等核心数据 |
| Soul | 灵魂的运行时对象，对应 Ghost.soul |
| InnateTendency | 天生倾向性，6维人格属性 |
| Sensibility | 感性系统，对主人的爱恨程度 |
| Rationality | 理性系统，情感回弹力 |
| Curiosity | 好奇心系统，主动认知世界的机制 |
| Impression | 主人印象，对用户的人格评分和文字描述 |
| ContextWindow | 内存中的对话上下文窗口 |
| Provider | AI 服务抽象，支持多种模型 |
