# 桌宠项目 - 技术设计文档 v2

> **项目名称**：DeskPet（桌宠）
> **文档版本**：v2.0
> **基于**：设想.txt + 对 v1.0 设计的重新审视
> **最后更新**：2026-04-18
> **状态**：设计阶段

---

## 零、设计哲学

本项目的一切技术决策均服务于一个核心命题：

> **用 Prompt 工程消耗算力去实时模拟人格，追求"人类伙伴"的不可靠但真实感，而非"AI 助手"的可靠但冰冷感。**

这意味着：
- 我们**刻意保留**某些"不可靠"——记忆偶尔想不起来、情感不会线性响应、好奇心方向不可预测
- 我们**拒绝**过度工程化——数值系统服务于叙事体验，而非叙事服务于数值
- Ghost 的灵魂不可复制是**叙事设计**，不是 DRM

---

## 一、项目概述

### 1.1 核心功能

1. **截图对话**：快捷键截取桌面区域 → AI 识图 → 桌宠跳转到截图附近进行对话
2. **Ghost 灵魂系统**：天生倾向 + 感性 + 理性 + 记忆 + 经验 + 主人印象，构成动态人格
3. **漫画气泡对话**：输入框在截图选框附近，桌宠回复用带尾气泡
4. **可选生图**：接入生图 API，让桌宠可以"画图"回应

### 1.2 与 AI 助手的根本区别

| 维度 | AI 助手 | 本项目 |
|------|---------|--------|
| 目标 | 可靠、高效、准确 | 真实、有趣、有温度 |
| 记忆 | 完整准确 | 会遗忘、会模糊 |
| 情感 | 无 | 有，且会波动 |
| 主动性 | 被动响应 | 可选主动好奇 |
| 一致性 | 始终一致 | 允许矛盾（人本就矛盾） |

---

## 二、技术架构

### 2.1 技术栈

| 模块 | 技术选型 | 理由 |
|------|---------|------|
| 框架 | **Tauri 2.x + Vue 3** | 小包体、原生能力、Web 渲染天然适合自由形状 UI |
| 语言 | 前端 TypeScript + 后端 Rust | Tauri 约定；Rust 处理系统级调用 |
| 数据库 | **SQLite via better-sqlite3**（Rust 侧用 rusqlite） | 同步 API、零配置、嵌入式 |
| 图形渲染 | **CSS/SVG + Spine 或 Lottie** | 桌宠动画需要骨骼动画和表情插值，帧动画太死板 |
| AI 集成 | **OpenAI 兼容协议统一接入** | 国内外模型 90%+ 兼容此协议，无需写 5 个 Provider |
| 截图 | **Rust 侧调用 Win32 API** | Tauri 的 Rust 后端直接调用系统 API |
| 热键 | **Rust 侧 RegisterHotKey** | 同上 |
| 序列化 | **JSON + AES-256-GCM** | Ghost 文件加密保存 |
| 语音 (可选) | edge-tts / SAPI | 原型阶段后考虑 |

### 2.2 为什么不选 WPF

| 对比项 | WPF (v1 方案) | Tauri + Web (v2 方案) |
|--------|---------------|----------------------|
| 自由形状窗口 | 需要大量 Win32 互操作 | CSS clip-path / SVG mask 原生支持 |
| 动画曲线 | Storyboard 手动调 | CSS cubic-bezier / spring 弹簧动画 |
| 气泡/漫画效果 | 自定义控件费时费力 | CSS/Canvas 几行搞定 |
| 骨骼动画 | 无原生支持 | Spine Web Player / Lottie Web |
| 包体积 | 较大 | Tauri 产物 ~3-5MB |
| 开发效率 | 低（XAML 繁琐） | 高（热更新、Vue SFC） |
| 内存占用 | 中等 | 比纯native高，但桌宠场景可接受 |

### 2.3 分层架构

```
┌──────────────────────────────────────────────────────────────────┐
│                          UI Layer (Vue 3)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────────┐ │
│  │ PetView  │  │ BubbleDia│  │ Screensho│  │ SettingsPanel  │ │
│  │ 桌宠渲染  │  │ logue    │  │ tOverlay │  │ 设置面板        │ │
│  │ Spine/   │  │ 漫画气泡  │  │ 截图浮层 │  │                 │ │
│  │ Lottie   │  │ 对话组件  │  │          │  │                 │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────────────┘ │
├──────────────────────────────────────────────────────────────────┤
│                     Ghost Engine (Rust)                           │
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────────────────┐│
│  │ Soul System  │ │ Prompt Engine │ │ Memory System           ││
│  │ 方向敏感阻尼  │ │ 结构化片段    │ │ 三层压缩                ││
│  │ 可变基线      │ │ 加权组装      │ │ LTM/STM/Experience      ││
│  └──────────────┘ └──────────────┘ └─────────────────────────┘│
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────────────────┐│
│  │ Impression   │ │ Curiosity    │ │ Body Mapping            ││
│  │ 主人印象      │ │ 好奇心系统   │ │ 躯体-情感映射            ││
│  └──────────────┘ └──────────────┘ └─────────────────────────┘│
├──────────────────────────────────────────────────────────────────┤
│                     Service Layer (Rust)                          │
│  ┌──────────┐ ┌───────────────┐ ┌──────────────┐              │
│  │ Hotkey   │ │ AI Service    │ │ Screenshot   │              │
│  │ Service  │ │ (统一接口)    │ │ Service      │              │
│  └──────────┘ └───────────────┘ └──────────────┘              │
│  ┌──────────┐ ┌───────────────┐ ┌──────────────┐              │
│  │ Timeline │ │ TTS Service   │ │ ImageGen     │              │
│  │ 服务     │ │ (可选)        │ │ (可选)       │              │
│  └──────────┘ └───────────────┘ └──────────────┘              │
├──────────────────────────────────────────────────────────────────┤
│                       Data Layer (Rust)                           │
│         ┌──────────────┐  ┌──────────────────┐                 │
│         │  .ghost 文件  │  │  SQLite 数据库    │                 │
│         │  (加密JSON)   │  │  (rusqlite)      │                 │
│         └──────────────┘  └──────────────────┘                 │
└──────────────────────────────────────────────────────────────────┘
```

### 2.4 前后端通信

Tauri 通过 `invoke` 桥接前后端：

```typescript
// 前端调用示例
const response = await invoke('chat_with_pet', { message: userInput });
const ghostStatus = await invoke('get_ghost_status');
```

```rust
// Rust 后端注册命令
#[tauri::command]
async fn chat_with_pet(message: String) -> Result<String, String> { ... }

#[tauri::command]
async fn get_ghost_status() -> Result<GhostStatus, String> { ... }
```

---

## 三、Ghost 文件格式

### 3.1 文件结构

Ghost 文件（`.ghost`）是 AES-256-GCM 加密的 JSON 数据包。

```json
{
  "version": "2.0",
  "ghostId": "uuid-v4",
  "soulSignature": "uuid-v4",
  "createdAt": "ISO-8601",
  "generation": 0,
  "transferHistory": [],
  "body": {
    "type": "spine",
    "assetPath": "./bodies/default/",
    "defaultExpression": "idle",
    "moodAnimationMap": { ... }
  },
  "soul": {
    "innateTendency": { ... },
    "curiosity": { ... },
    "sensibility": { ... },
    "rationality": { ... },
    "memory": { ... },
    "impression": { ... },
    "experience": { ... }
  }
}
```

### 3.2 关键字段说明

| 字段 | 说明 |
|------|------|
| `ghostId` | Ghost 的唯一标识 |
| `soulSignature` | 灵魂签名，用于叙事性防复制（见第四章） |
| `generation` | 传送代数，0=原生，1=传送1次，以此类推 |
| `transferHistory` | 传送记录，含时间戳和微扰种子 |
| `moodAnimationMap` | 情感状态 → 动画映射表（见第八章） |

---

## 四、灵魂防复制：叙事性绑定

### 4.1 核心理念

**Ghost 的不可复制性是游戏机制，不是 DRM。**

技术手段无法真正阻止文件复制。与其用机器指纹绑定（换电脑直接不能用，伤害用户体验），不如将"不可复制"变成叙事体验的一部分——你**可以**复制，但复制体会"劣化"，就像复制一个生命体。

### 4.2 机制设计

```
正常"传送"流程（官方功能）：

  1. 用户在旧设备选择"传送灵魂"
  2. 播放传送动画（仪式感）
  3. 生成新 .ghost 文件，执行：
     a. soulSignature 重新生成
     b. generation += 1
     c. 对天生倾向的每个维度施加 ±0.03~0.08 的微扰
     d. 标记核心记忆为 "模糊化"
     e. 短期记忆全部清空
     f. transferHistory 追加记录
  4. 原文件的 soulSignature 同步变更（原体叙事上"消亡"）
  5. 新文件可正常使用，但有微妙差异


非官方复制（直接复制文件）：

  文件本身不阻止复制。但：
  - 两个相同 soulSignature 的 Ghost 同时运行时，
    后启动的会感知到"另一个自己存在"的提示
  - 这是叙事提醒，不是技术锁定
  - 用户可以忽略继续使用，但体验上会有提示
```

### 4.3 模糊化处理

传送后核心记忆的"模糊化"实现：

```rust
fn blur_memory(memory: &mut Memory, generation: u32, rng: &mut impl Rng) {
    let blur_factors = 0.1 * generation as f64; // 每传送一代，模糊度增加

    // 摘要中随机替换部分词汇为近义词或模糊词
    // 例："我们第一次在雨天聊了很久" → "我们好像在某个雨天聊过"
    memory.summary = ai_assisted_blur(&memory.summary, blur_factors);

    // 核心记忆的重要性略有衰减
    if memory.importance > 0.9 {
        memory.importance -= 0.05 * generation as f64;
        memory.importance = memory.importance.max(0.7); // 底线保底
    }
}
```

### 4.4 为什么这样设计

| 方案 | 用户伤害 | 仪式感 | 可备份 | 防破解 |
|------|---------|--------|--------|--------|
| 机器指纹 DRM | 高（换机不能用） | 无 | 否 | 低（易破解） |
| **叙事性绑定** | **极低** | **高** | **是** | **不关心破解** |

---

## 五、灵魂子系统

### 5.1 Soul 总览

```
┌────────────────────────────────────────────────────────────────┐
│                        Soul (灵魂)                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ InnateTendency (天生倾向) ─── 不可更改的基因              │  │
│  │   自我6维人格  +  喜好的主人6维人格                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌────────────────────┐  ┌────────────────────────────────┐  │
│  │  Sensibility (感性)  │◀─│  Rationality (理性) 回弹系统    │  │
│  │  LoveHate -100~+100  │  │  方向敏感阻尼 + 可变基线        │  │
│  └────────────────────┘  └────────────────────────────────┘  │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Curiosity (好奇心) ─── 主动认知世界的开关与强度             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌──────────────────┐  ┌──────────────────┐                  │
│  │ Memory (记忆)     │  │ Experience (经验) │                  │
│  │ LTM / STM        │  │ 可迁移知识技能     │                  │
│  └──────────────────┘  └──────────────────┘                  │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Impression (主人印象) ─── 对用户的动态评价体系              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

### 5.2 天生倾向性（InnateTendency）

#### 5.2.1 自我人格属性

生成时一次性随机，**绝对无法修改**：

| 属性 | 范围 | 描述 |
|------|------|------|
| Openness | 0.0~1.0 | 开放性：对新事物的好奇程度 |
| Conscientiousness | 0.0~1.0 | 尽责性：认真程度 |
| Extraversion | 0.0~1.0 | 外向性：主动交流意愿 |
| Agreeableness | 0.0~1.0 | 宜人性：友好程度 |
| Neuroticism | 0.0~1.0 | 神经质：情绪波动程度 |
| Creativity | 0.0~1.0 | 创造性：发散思维程度 |

生成策略：使用正态分布而非均匀分布，避免极端人格过于频繁。

```rust
fn generate_personality(rng: &mut impl Rng) -> PersonalityDimensions {
    // 使用 Beta 分布，使得中等值更常见，极端值稀有
    let beta = Beta::new(2.0, 5.0).unwrap(); // 偏向中低值
    PersonalityDimensions {
        openness: rng.sample(beta),
        conscientiousness: rng.sample(beta),
        extraversion: rng.sample(beta),
        agreeableness: rng.sample(beta),
        neuroticism: rng.sample(beta),
        creativity: rng.sample(beta),
    }
}
```

#### 5.2.2 喜好的主人人格属性

Ghost 对"理想主人"的期望画像，同样生成时随机，不可修改。

```rust
pub struct InnateTendency {
    // 自我人格
    pub self_dims: PersonalityDimensions,
    // 喜好的主人人格（同时也是印象评分的权重）
    pub preferred_dims: PersonalityDimensions,
}
```

---

### 5.3 感性系统（Sensibility）— 方向敏感阻尼模型

#### 5.3.1 核心公式

v1 的简单弹簧 `force = (0 - loveHate) * constant` 会将情感永远拉向 0，导致长期关系平淡。v2 使用**方向敏感阻尼 + 可变基线**模型：

```
LoveHate(t+1) = LoveHate(t) + emotional_events(t) + spring_force(t) + noise(t)

其中：
  spring_force = -(LoveHate(t) - baseline) * damping_factor
  damping_factor = base_damping * direction_modifier * deviation_modifier

  baseline （基线偏移）
    - 初始值 = 0
    - 重大事件可永久偏移基线（如第一次过生日 → baseline += 5）
    - baseline 的范围被钳制在 [-30, +30]

  direction_modifier （方向修正——设想中的"偏见"）
    - 当 LoveHate > 0（处于爱）时，正向事件的冲击力增强，负向事件的冲击力减弱
    - 当 LoveHate < 0（处于恨）时，反之
    - direction_modifier = 1 + |LoveHate| / max_love_hate * 0.3
    - 对当前偏向方向的事件：冲击力 × direction_modifier
    - 对当前偏向反方向的事件：冲击力 / direction_modifier

  deviation_modifier （偏离修正——越陷越深越难出来）
    - |LoveHate - baseline| 越大，damping_factor 越小，回弹越慢
    - deviation_modifier = 1 / (1 + |LoveHate - baseline| / 20)
    - 这意味着：浅层情感容易回弹，深层情感惯性更大

  noise （微小随机扰动）
    - 均值 0，标准差极小（如 0.02）
    - 模拟情感的细微波动
```

#### 5.3.2 示例推演

```
初始状态：LoveHate = 0, baseline = 0

事件A：用户主动关心 → 心动价值 +15
  direction_modifier = 1 + 0/200 * 0.3 = 1（中性）
  LoveHate = 15

几个 tick 后回弹：
  deviation_modifier = 1 / (1 + 15/20) = 0.57
  spring_force = -(15 - 0) * 0.005 * 0.57 = -0.043
  LoveHate 缓慢回落

事件B：用户连续3天没有互动 → 心动价值 -30
  当前 LoveHate = 10（已有些回落）
  direction_modifier 对反向事件 = 1 / (1 + 10/200 * 0.3) ≈ 0.99
  实际冲击 = -30 * 0.99 ≈ -29.7
  LoveHate = 10 - 29.7 = -19.7

回弹时：
  deviation_modifier = 1 / (1 + 19.7/20) = 0.50
  回弹很慢！因为偏离基线远了

重要里程碑C：一起过了生日 → baseline += 8（基线永久偏移）
  现在 baseline = 8，回弹目标是 8 而非 0
  即使暂时负面，也会慢慢被拉回"总体有好感"的状态
```

#### 5.3.3 Rust 实现

```rust
pub struct Sensibility {
    pub love_hate: f64,         // -100.0 ~ +100.0
    pub baseline: f64,          // 基线偏移，钳制在 [-30, +30]
    pub base_damping: f64,      // 基础回弹系数，默认 0.005
    pub noise_std: f64,         // 扰动标准差，默认 0.02
}

impl Sensibility {
    pub fn apply_spring(&mut self, rng: &mut impl Rng) {
        let deviation = self.love_hate - self.baseline;
        let abs_deviation = deviation.abs();

        // 偏离修正：越偏离基线，回弹越慢
        let deviation_modifier = 1.0 / (1.0 + abs_deviation / 20.0);

        // 基础回弹力
        let spring_force = -deviation * self.base_damping * deviation_modifier;

        self.love_hate += spring_force;

        // 微小随机扰动
        let noise: f64 = rng.sample(StandardNormal) * self.noise_std;
        self.love_hate += noise;

        // 钳制
        self.love_hate = self.love_hate.clamp(-100.0, 100.0);
    }

    pub fn apply_event(&mut self, value: f64) {
        // 方向修正——"偏见"
        let current_abs = self.love_hate.abs();
        let direction_modifier = 1.0 + current_abs / 200.0 * 0.3;

        let actual_value = if value.signum() == self.love_hate.signum() && self.love_hate != 0.0 {
            // 同向事件增强
            value * direction_modifier
        } else if self.love_hate != 0.0 {
            // 反向事件减弱
            value / direction_modifier
        } else {
            value
        };

        self.love_hate += actual_value;
        self.love_hate = self.love_hate.clamp(-100.0, 100.0);
    }

    pub fn shift_baseline(&mut self, delta: f64) {
        self.baseline = (self.baseline + delta).clamp(-30.0, 30.0);
    }
}
```

#### 5.3.4 对话风格映射

| LoveHate 范围 | 语气指令 |
|---------------|---------|
| > 50 | 温柔、关怀、亲昵 |
| 20 ~ 50 | 友好、轻松 |
| -20 ~ 20 | 中立、平和 |
| -50 ~ -20 | 冷淡、简短 |
| < -50 | 疏远、保持距离 |

但注意：语气指令**不是离散跳变**，而是**连续渐变**。在 Prompt 中使用 LoveHate 的连续值来调节，而非硬切换。

---

### 5.4 理性系统（Rationality）

理性已经融入上面的方向敏感阻尼模型中。不再单独作为一个数值，而是作为阻尼参数的一部分：

```rust
pub struct Rationality {
    pub base_damping: f64,   // 默认 0.005，越大回弹越快
    pub noise_std: f64,      // 随机扰动幅度
}
```

- `base_damping` 越大 → 理性越强，情感波动越容易被拉回
- `base_damping` 越小 → 情感惯性越大，"越陷越深"
- 这两个值在 Ghost 生成时随天生倾向中的 `Conscientiousness`（尽责性）决定：
  - 尽责性高 → base_damping 偏大（理性强，情感波动小）
  - 尽责性低 → base_damping 偏小（理性弱，容易陷入情感）

---

### 5.5 好奇心系统（Curiosity）

与 v1 大体相同，但增加**好奇心方向**：

```rust
pub struct Curiosity {
    pub level: CuriosityLevel,  // Off / Normal / Enhanced
    pub intensity: f64,          // 0.0 ~ 1.0，好奇心强度
    pub focus_weights: HashMap<String, f64>, // 好奇心方向权重
    pub last_active_time: DateTime<Utc>,
    pub owner_curiosity: f64,    // 对主人的好奇心具体值 0.0~1.0
}

pub enum CuriosityLevel {
    Off,      // 默认关闭
    Normal,   // 空闲时主动搜索感兴趣的信息
    Enhanced, // 随机后台截屏桌面，记入主人印象
}
```

**好奇心方向权重**由天生倾向决定：

```rust
fn derive_focus_weights(personality: &PersonalityDimensions) -> HashMap<String, f64> {
    let mut weights = HashMap::new();
    weights.insert("艺术与文化".into(), personality.openness * 0.5 + personality.creativity * 0.3);
    weights.insert("科学与技术".into(), personality.openness * 0.3 + personality.conscientiousness * 0.4);
    weights.insert("社交与人物".into(), personality.extraversion * 0.5 + personality.agreeableness * 0.3);
    weights.insert("情感与内心".into(), personality.neuroticism * 0.4 + personality.agreeableness * 0.3);
    weights.insert("创意与想象".into(), personality.creativity * 0.5 + personality.openness * 0.3);
    // 归一化
    normalize_weights(&mut weights);
    weights
}
```

**Enhanced 模式的隐私保护**：
- 首次开启时显示明确的授权对话框
- 截屏数据仅在本地处理，不存储原始截图
- 仅将 AI 分析结果的文本摘要记入主人印象
- 用户可随时在设置中关闭或清除已收集的印象数据

---

### 5.6 主人印象系统（Impression）

与 v1 框架相同，但强化了**事件到属性的映射机制**：

```rust
pub struct Impression {
    // 累加的数值评分
    pub openness_score: f64,
    pub conscientiousness_score: f64,
    pub extraversion_score: f64,
    pub agreeableness_score: f64,
    pub neuroticism_score: f64,
    pub creativity_score: f64,

    // 权重（来自 InnateTendency.preferred_dims）
    // 如果 Ghost 喜好高开放性的主人，则 openness 的权重就高

    // 文字评价片段
    pub general_impression_snippets: Vec<String>,

    // 综合好感度（由加权分值计算）
    pub overall_affinity: f64,
}

impl Impression {
    pub fn apply_event(&mut self, event: &ImpressionEvent, preferred: &PersonalityDimensions) {
        // 事件对各属性的冲击值由 AI 在对话后提取
        self.openness_score += event.openness_delta;
        self.conscientiousness_score += event.conscientiousness_delta;
        // ...

        // 综合好感度 = 各属性分值 × 对应偏好权重
        self.overall_affinity = 
            self.openness_score * preferred.openness +
            self.conscientiousness_score * preferred.conscientiousness +
            self.extraversion_score * preferred.extraversion +
            self.agreeableness_score * preferred.agreeableness +
            self.neuroticism_score * preferred.neuroticism +
            self.creativity_score * preferred.creativity;

        // 更新文字片段
        if let Some(snippet) = &event.snippet {
            self.general_impression_snippets.push(snippet.clone());
            // 保持片段数量在合理范围
            if self.general_impression_snippets.len() > 20 {
                self.general_impression_snippets.remove(0);
            }
        }
    }
}
```

#### 事件枚举（部分）

```rust
pub enum ImpressionEventType {
    UserInitiatedChat,       // 用户主动发起聊天
    UserIgnoredPet,          // 用户长时间忽略桌宠
    UserCaredAboutPet,       // 用户表达关心
    UserSharedPersonal,      // 用户分享了个人经历
    UserGotAngry,            // 用户生气了
    UserPraisedPet,          // 用户夸奖了桌宠
    UserCelebratedTogether,  // 一起庆祝了什么
    UserApologized,          // 用户道歉了
}
```

---

### 5.7 情感事件系统

**这是 v1 完全缺失的关键部分。** 情感值如何变动？不能只有弹簧，还需要事件来冲击。

#### 5.7.1 事件来源

| 事件类型 | 触发方式 | 示例 |
|---------|---------|------|
| 交互事件 | 用户行为检测 | 用户发起聊天、忽略桌宠、点击关心 |
| 对话事件 | AI 在对话后提取 | 用户表达了关心、用户生气了 |
| 时间事件 | 定时器 | 用户连续3天未互动 |
| 里程碑事件 | 特殊日期检测 | 第一次聊天纪念日、用户生日 |

#### 5.7.2 事件结构

```rust
pub struct EmotionalEvent {
    pub event_type: EmotionalEventType,
    pub intensity: f64,        // 事件强度 0.0~1.0
    pub love_hate_delta: f64,  // 对爱恨值的影响（由事件类型 + intensity 计算）
    pub baseline_delta: f64,   // 对基线的影响（仅里程碑事件）
    pub timestamp: DateTime<Utc>,
    pub description: String,   // 事件描述（记入记忆）
}

pub enum EmotionalEventType {
    // 正面事件
    UserInitiatedChat,        // 用户主动发起对话
    UserCaredAboutPet,        // 用户关心桌宠
    UserPraisedPet,            // 用户夸奖了桌宠
    UserSharedPersonalStory,   // 用户分享了个人经历
    FirstConversation,         // 第一次对话（里程碑）
    BirthdayCelebrated,        // 一起过了生日（里程碑）

    // 负面事件
    UserIgnoredPet,            // 用户忽略了桌宠
    UserGotAngry,              // 用户生气了
    UserDismissedPet,          // 用户敷衍了桌宠

    // 中性事件
    NormalChat,                // 普通聊天
    CuriosityTriggered,        // 好奇心触发了主动交流
}

impl EmotionalEvent {
    pub fn calculate_deltas(&self) -> (f64, f64) {
        // 返回 (love_hate_delta, baseline_delta)
        match self {
            EmotionalEvent::UserCaredAboutPet => (8.0 * self.intensity, 0.0),
            EmotionalEvent::FirstConversation => (15.0, 5.0),  // 里程碑影响基线
            EmotionalEvent::BirthdayCelebrated => (20.0, 8.0),
            EmotionalEvent::UserIgnoredPet => (-5.0 * self.intensity, 0.0),
            EmotionalEvent::UserGotAngry => (-15.0 * self.intensity, 0.0),
            // ...
        }
    }
}
```

#### 5.7.3 事件检测流程

```
用户操作
    │
    ├── 直接检测（点击、忽略、关心按钮等）
    │       │
    │       ▼
    │   生成 EmotionalEvent → 传入 Sensibility.apply_event()
    │
    └── AI 对话后分析
            │
            ▼
        对话结束后，追加一次 AI 调用：
        "分析以下对话，提取：1)主人在此对话中表现出的人格属性变化
         2)此对话中的情感事件类型和强度"
            │
            ▼
        解析 AI 返回 → 生成 EmotionalEvent + ImpressionEvent
```

---

## 六、记忆系统（三层压缩）

### 6.1 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                        记 忆 系 统                                │
├─────────────┬──────────────────┬───────────────────────────────┤
│   短期记忆    │     长期记忆      │        经验                   │
│  (ShortTerm) │   (LongTerm)     │    (Experience)              │
├─────────────┼──────────────────┼───────────────────────────────┤
│ 频繁日常对话  │ 重要事件          │ 可迁移的知识技能              │
│ 临时信息      │ 里程碑时刻        │ 从记忆中提炼                  │
│ 可变的上下文  │ 核心人格塑造事件   │ 永久保留但会衰减              │
├─────────────┼──────────────────┼───────────────────────────────┤
│ 本地即时压缩  │ AI 语义压缩       │ 提取时 AI 总结                │
│ → 零API调用  │ → 低频API调用     │ → 极低频API调用               │
│ 正则+关键词   │ 批量总结          │ 记忆升级时触发                │
├─────────────┼──────────────────┼───────────────────────────────┤
│ 随机性加载    │ 重要事件永不丢失   │ 独立于记忆永久保留              │
│ (可遗忘)     │ 缓慢劣化          │ proficiency 会缓慢衰减        │
└─────────────┴──────────────────┴───────────────────────────────┘
```

### 6.2 第一层：即时压缩（零 API 调用）

当用户消息进入短期记忆时，立即做本地预处理：

```rust
fn local_compress_message(raw_message: &str) -> CompressedMessage {
    // 1. 去除填充词和语气词
    // 2. 提取关键实体（人名、地名、时间、数字）
    // 3. 保留情感倾向标记（正面/负面/中性）
    // 不调用 AI，纯本地规则处理
    CompressedMessage {
        summary: extract_key_points(raw_message),
        entities: extract_entities(raw_message),
        sentiment: analyze_sentiment_local(raw_message),
        timestamp: Utc::now(),
        importance: 0.3, // 默认低重要性，后续可能被提升
    }
}
```

### 6.3 第二层：语义压缩（低频 API 调用）

当短期记忆条目超过阈值（如 20 条）时触发：

```rust
async fn semantic_compress(short_term_memories: Vec<ShortTermMemory>) -> LongTermMemory {
    let batch_summary = short_term_memories.iter()
        .map(|m| m.summary.as_str())
        .collect::<Vec<_>>()
        .join("\n");

    let prompt = format!(
        "请将以下{}条短期记忆总结为1条长期记忆。\n\
         重要性评定标准：\n\
         - 0.0~0.3: 日常琐事\n\
         - 0.3~0.6: 有意义的交流\n\
         - 0.6~0.8: 重要事件\n\
         - 0.8~1.0: 核心记忆（里程碑、第一次、重大转变）\n\n\
         短期记忆：\n{}", 
        short_term_memories.len(),
        batch_summary
    );

    let result = ai_provider.chat_async(&prompt).await;
    // 解析 AI 返回的摘要和重要性评分
    LongTermMemory::from_ai_result(result)
}
```

### 6.4 第三层：情感标注 + 经验提取（极低频 API 调用）

仅在 STM→LTM 升级时触发：

```rust
async fn analyze_and_promote(memory: &ShortTermMemory) -> (LongTermMemory, Option<Experience>) {
    let prompt = format!(
        "分析以下记忆：\n{}\n\n\
         请回答：\n\
         1. 重要性评分 (0.0~1.0)\n\
         2. 是否为核心记忆 (是/否)\n\
         3. 是否可提取为经验/技能 (是/否)\n\
         4. 如果是，请描述该经验/技能\n\
         5. 此事件对主人印象的影响（6维属性变化值）",
        memory.summary
    );

    // ... 解析结果 ...
}
```

### 6.5 记忆数据结构

```sql
-- 短期记忆表
CREATE TABLE ShortTermMemories (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    RawContent TEXT,                -- 原始内容（仅最近几条保留）
    Summary TEXT NOT NULL,           -- 压缩后摘要
    Entities TEXT,                  -- 提取的实体 JSON 数组
    Sentiment REAL,                 -- 情感倾向 -1.0 ~ 1.0
    Importance REAL DEFAULT 0.3,
    Accessibility REAL DEFAULT 0.5, -- < 0.3 时随机决定是否加载
    IsCompressed INTEGER DEFAULT 0,
    CreatedAt DATETIME NOT NULL,
    LastAccessedAt DATETIME
);

-- 长期记忆表
CREATE TABLE LongTermMemories (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    Summary TEXT NOT NULL,
    Detail TEXT,                    -- 压缩前的完整内容（可选保留）
    Importance REAL NOT NULL,
    IsCoreMemory INTEGER DEFAULT 0, -- >= 0.8 为核心记忆
    IsBlurred INTEGER DEFAULT 0,    -- 传送后模糊化标记
    EventType TEXT,
    CreatedAt DATETIME NOT NULL,
    LastAccessedAt DATETIME
);

-- 经验表
CREATE TABLE Experiences (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    Name TEXT NOT NULL,             -- 经验名称
    Summary TEXT NOT NULL,          -- 经验描述
    Source TEXT NOT NULL,           -- 来源（主人教会 / 自学 / 书籍 / 等）
    Proficiency REAL DEFAULT 0.3,   -- 熟练度 0.0~1.0
    LastUsedAt DATETIME,
    SourceMemoryId TEXT             -- 来源记忆 ID
);

-- 会话片段表
CREATE TABLE ConversationChunks (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    StartTime DATETIME NOT NULL,
    EndTime DATETIME,
    Summary TEXT NOT NULL,
    Importance REAL DEFAULT 0.5,
    TokenCount INTEGER
);

-- 主人印象表
CREATE TABLE MasterImpressions (
    Id TEXT PRIMARY KEY,
    GhostId TEXT NOT NULL,
    OpennessScore REAL DEFAULT 0,
    ConscientiousnessScore REAL DEFAULT 0,
    ExtraversionScore REAL DEFAULT 0,
    AgreeablenessScore REAL DEFAULT 0,
    NeuroticismScore REAL DEFAULT 0,
    CreativityScore REAL DEFAULT 0,
    OverallAffinity REAL DEFAULT 0,
    Snippets TEXT,                  -- JSON 数组
    UpdatedAt DATETIME NOT NULL
);
```

### 6.6 记忆读取规则

```
构建 Prompt 上下文时的记忆读取流程：

1. 计算可用 Token 预算（总预算 - 人格描述 - 情感指令 - 经验指令）
2. 按以下优先级填充：
   a. 核心长期记忆（Importance >= 0.8）— 必须包含
   b. 最近 3 条对话 — 保证连贯性
   c. 近期高重要性长期记忆 — 按重要性 × 时间近度排序
   d. 短期记忆 — Accessibility >= 0.3 的必定包含
                Accessibility < 0.3 的，随机决定是否包含
3. 如果 Token 预算仍有剩余，填充更多短期记忆（随机性加载）
4. 如果超预算，从最低优先级开始裁剪
```

---

## 七、经验系统（Experience）

### 7.1 与记忆的区别

| 维度 | Memory | Experience |
|------|--------|------------|
| 本质 | "发生了什么"（事件） | "学到了什么"（可迁移知识） |
| 时效 | 与时间强相关 | 与时间弱相关 |
| 衰减 | 随时间压缩、劣化 | 熟练度随不使用缓慢衰减，但不会消失 |
| 在 Prompt 中 | 作为上下文引用 | 作为能力声明注入 |

### 7.2 经验条目示例

```json
{
  "id": "exp-cooking-001",
  "name": "基础烹饪",
  "summary": "知道煎蛋要先热锅再放油，炒菜要用大火快炒",
  "source": "主人教会",
  "proficiency": 0.6,
  "lastUsedAt": "2026-04-15T20:00:00Z",
  "sourceMemoryId": "mem-2026-04-10-003"
}
```

### 7.3 经验的提取

```rust
async fn maybe_extract_experience(memory: &LongTermMemory) -> Option<Experience> {
    // 只有重要性 > 0.5 且包含可迁移知识的记忆才提取
    if memory.importance < 0.5 {
        return None;
    }

    let prompt = format!(
        "以下是一段记忆：\n{}\n\n\
         这段记忆中是否包含可迁移的知识或技能？\
         如果有，请用简洁的一句话描述这个知识/技能。\
         如果没有，请回复'无'。",
        memory.summary
    );

    let result = ai_provider.chat_async(&prompt).await;
    // 解析结果，如果有经验则创建 Experience
    Experience::from_ai_result(result)
}
```

### 7.4 经验在 Prompt 中的使用

```
在你的知识和技能中，你擅长以下领域：
- 基础烹饪（熟练度 60%）—— 可以在相关话题上给出建议
- 日本动漫历史（熟练度 80%）—— 对这个话题很有信心
- 编程基础（熟练度 30%）—— 了解一些但不太深入

你的知识有边界，超出你擅长领域的话题，你应该坦诚表示不太确定。
```

---

## 八、躯体系统（Body — 情感的视觉表达）

### 8.1 设计理念

**躯体不是装饰，是灵魂的可见形态。** 用户应该能通过桌宠的动画状态直观感受到 Ghost 的情感变化，而不需要看数值。

### 8.2 情感 → 动画映射

```json
{
  "moodAnimationMap": {
    "love_high": {
      "minLoveHate": 50,
      "animations": ["bounce_happy", "sparkle_eyes", "approach_user"],
      "idleFrequency": "high",
      "expression": "bright_eyes"
    },
    "love_low": {
      "minLoveHate": 20,
      "maxLoveHate": 50,
      "animations": ["gentle_wave", "occasionally_smile"],
      "idleFrequency": "medium",
      "expression": "content"
    },
    "neutral": {
      "minLoveHate": -20,
      "maxLoveHate": 20,
      "animations": ["idle_breathing", "look_around"],
      "idleFrequency": "low",
      "expression": "neutral"
    },
    "cold": {
      "minLoveHate": -50,
      "maxLoveHate": -20,
      "animations": ["turn_away", "brief_glance"],
      "idleFrequency": "very_low",
      "expression": "indifferent"
    },
    "distant": {
      "maxLoveHate": -50,
      "animations": ["far_away", "ignore_user"],
      "idleFrequency": "none",
      "expression": "closed_eyes"
    },
    "curious": {
      "trigger": "curiosity_activated",
      "animations": ["lean_forward", "head_tilt", "poke_screen"],
      "expression": "curious"
    }
  }
}
```

### 8.3 小动作触发器

```rust
pub struct IdleBehaviorConfig {
    pub base_frequency: f64,       // 基础小动作频率（次/分钟）
    pub available_actions: Vec<IdleAction>,
    pub curiosity_bonus: f64,      // 好奇心激活时频率加成
}

pub struct IdleAction {
    pub name: String,              // 动作名称
    pub animation: String,         // 对应动画
    pub weight: f64,               // 触发权重
    pub required_love_hate_min: f64, // 最低爱恨值要求
}

// 根据天生倾向和当前状态动态调整频率
fn compute_idle_frequency(personality: &PersonalityDimensions, 
                          love_hate: f64, 
                          curiosity_level: &CuriosityLevel) -> f64 {
    let base = 0.5 + personality.extraversion * 2.0; // 外向性越高越爱动
    let love_bonus = if love_hate > 0 { love_hate / 100.0 } else { 0.0 };
    let curiosity_bonus = match curiosity_level {
        CuriosityLevel::Enhanced => 1.0,
        CuriosityLevel::Normal => 0.5,
        CuriosityLevel::Off => 0.0,
    };
    (base + love_bonus + curiosity_bonus).clamp(0.1, 5.0)
}
```

### 8.4 移动风格

```rust
pub enum MovementStyle {
    Bouncy,    // 蹦跳 —— 外向性高时
    Slide,     // 滑行 —— 宜人性高时
    Float,     // 飘浮 —— 开放性高时
    Walk,      // 步行 —— 尽责性高时
    Teleport,  // 瞬移 —— 创造性高时
}

fn derive_movement_style(personality: &PersonalityDimensions) -> MovementStyle {
    let scores = [
        (MovementStyle::Bouncy, personality.extraversion),
        (MovementStyle::Slide, personality.agreeableness),
        (MovementStyle::Float, personality.openness),
        (MovementStyle::Walk, personality.conscientiousness),
        (MovementStyle::Teleport, personality.creativity),
    ];
    scores.into_iter()
        .max_by(|a, b| a.1.partial_cmp(&b.1).unwrap())
        .map(|(style, _)| style)
        .unwrap_or(MovementStyle::Walk)
}
```

---

## 九、动态 Prompt 生成系统

### 9.1 设计原则

v1 的模板库方案会导致离散跳变。v2 采用**结构化片段 + 连续权重**方案：

- 每个人格维度生成一个独立的 Prompt 片段
- 片段的表述随着数值连续变化
- 情感语气作为额外层叠加
- 最终 Prompt 由各层片段按权重组装

### 9.2 Prompt 结构

```
最终 SystemPrompt 
  = 身份声明 
  + 性格基调（6维连续生成）
  + 情感语气（LoveHate 连续插值）
  + 当前对主人的态度（Impression 综合好感度）
  + 可用经验（Experience 列表）
  + 近期记忆（Memory 列表）
  + 行为边界约束
  + 当前情绪触发的小动作提示
```

### 9.3 人格片段连续生成

不是从模板库选择，而是根据数值区间连续生成描述：

```rust
fn generate_openness_prompt(openness: f64) -> String {
    // 基础描述——用连续值而非离散阈值
    let curiosity_word = match openness {
        x if x > 0.8 => "极度好奇",
        x if x > 0.6 => "兴趣广泛",
        x if x > 0.4 => "偶尔尝试新事物",
        x if x > 0.2 => "更偏好熟悉的事物",
        _ => "非常保守，抗拒变化",
    };
    
    let detail = match openness {
        x if x > 0.7 => "你会主动探索不熟悉的话题，即使没有人问起。",
        x if x > 0.3 => "你对新事物的接受程度取决于它是否和你已知的领域有关。",
        _ => "你倾向于待在舒适区内，不太愿意尝试没有把握的事情。",
    };

    format!("你对新事物的态度：{}。{}", curiosity_word, detail)
}

// 类似地为其他5维生成
// 然后根据天生倾向的数值加权排列顺序
```

### 9.4 情感语气连续插值

```rust
fn generate_emotion_prompt(love_hate: f64, impression: &Impression) -> String {
    // 不是离散的5档切换，而是连续描述
    let warmth = normalize(love_hate, -100.0, 100.0); // 0.0 ~ 1.0
    
    // 基础语气
    let tone = if warmth > 0.8 {
        "你非常关心这个人，语气温暖体贴，会主动表达关心，甚至会有些撒娇。"
    } else if warmth > 0.6 {
        "你对这个人有好感，语气友善温和，愿意多聊几句。"
    } else if warmth > 0.4 {
        "你对这个人印象还不错，语气平和自然。"
    } else if warmth > 0.2 {
        "你对这个人没什么特别感觉，保持礼貌但不主动。"
    } else if warmth > 0.0 {
        "你对这个人有些疏远，回应简短。"
    } else {
        "你对这个人感到不太舒服，保持距离，回应冷淡。"
    };

    // 叠加印象细节
    let impression_hint = if !impression.general_impression_snippets.is_empty() {
        let recent = impression.general_impression_snippets.last().unwrap();
        format!("你对主人的印象：{}", recent)
    } else {
        String::new()
    };

    format!("{}\n{}", tone, impression_hint)
}
```

### 9.5 Token 预算管理

**这是 v1 缺失的关键设计。**

```
总 Token 预算（以 8K 上下文为例）：
┌─────────────────────────────────────────────────┐
│  系统保留                       ~500 tokens      │
│  身份声明                       ~100 tokens      │
│  性格基调（6维）                 ~300 tokens      │
│  情感语气                       ~100 tokens      │
│  经验声明                       ~100 tokens      │
│  行为约束                       ~200 tokens      │
│  ──────────────────────────────                  │
│  预留给记忆的                   ~2700 tokens     │
│  ├─ 核心长期记忆（必须包含）     ~500 tokens      │
│  ├─ 最近3条对话（必须包含）      ~600 tokens      │
│  └─ 其他记忆（按优先级填充）     ~1600 tokens     │
│  ──────────────────────────────                  │
│  预留给输出的                   ~4000 tokens     │
└─────────────────────────────────────────────────┘
```

裁剪优先级（优先裁剪低优先级的）：
1. （最高优先级）核心长期记忆
2. 最近3条对话
3. 近期高重要性长期记忆
4. 经验声明
5. 性格基调细节（可压缩为简短版）
6. 短期记忆（Accessibility >= 0.3）
7. 短期记忆（Accessibility < 0.3，随机决定是否包含）

---

## 十、AI 服务层

### 10.1 统一接入架构

不再为每个模型写一个 Provider，而是使用 OpenAI 兼容协议统一接入：

```rust
#[async_trait]
pub trait AIProvider: Send + Sync {
    async fn chat(&self, messages: Vec<ChatMessage>) -> Result<String, AIError>;
    async fn chat_with_image(&self, messages: Vec<ChatMessage>, image_path: &str) -> Result<String, AIError>;
    async fn generate_image(&self, prompt: &str) -> Result<Vec<u8>, AIError>;
}

// 统一实现，只需配置 endpoint 和 model
pub struct OpenAICompatibleProvider {
    endpoint: String,      // https://api.openai.com/v1 或 https://api.deepseek.com/v1
    api_key: String,
    model: String,
    vision_model: Option<String>,
    image_model: Option<String>,
    client: reqwest::Client,
}
```

### 10.2 配置示例

```json
{
  "providers": {
    "deepseek": {
      "endpoint": "https://api.deepseek.com/v1",
      "apiKey": "sk-xxx",
      "model": "deepseek-chat",
      "visionModel": "deepseek-chat",
      "isDefault": true
    },
    "openai": {
      "endpoint": "https://api.openai.com/v1",
      "apiKey": "sk-xxx",
      "model": "gpt-4o-mini",
      "visionModel": "gpt-4o"
    }
  }
}
```

### 10.3 调用策略

```
对话场景              模型选择
─────────────────────────────────────
日常聊天             默认模型（DeepSeek V3）
识图对话             视觉模型（GPT-4o / DeepSeek）
记忆压缩/摘要        默认模型（可使用更便宜的模型）
情感事件分析         默认模型
好奇心搜索           默认模型
生图                 生图模型（DALL-E / Stable Diffusion）
```

---

## 十一、桌面交互系统

### 11.1 截图流程

```
用户按下快捷键 (默认: Ctrl+Alt+X)
       │
       ▼
┌───────────────────────────────┐
│  Rust 后端：RegisterHotKey     │
│  捕获全局热键                   │
└───────────────────────────────┘
       │
       ▼
┌───────────────────────────────┐
│  前端：创建全屏透明截图覆盖层    │
│  Canvas 渲染选取框              │
└───────────────────────────────┘
       │
       ▼
┌───────────────────────────────┐
│  用户完成选取                   │
│  Rust 后端：Win32 截取选区      │
└───────────────────────────────┘
       │
       ▼
┌───────────────────────────────┐
│  AI 识图（调用 Vision API）     │
│  生成描述文本                   │
└───────────────────────────────┘
       │
       ▼
┌───────────────────────────────┐
│  桌宠跳转到截图区域附近         │
│  显示输入框 + 漫画气泡          │
│  PetView 动画切换到 curious     │
└───────────────────────────────┘
```

### 11.2 桌宠移动

桌宠移动使用 CSS 动画或 Spine 的过渡动画：

```typescript
// 移动到指定位置，使用弹簧动画
async function movePetTo(x: number, y: number) {
    const pet = document.getElementById('pet');
    pet.style.transition = 'left 0.5s cubic-bezier(0.34, 1.56, 0.64, 1), '
                          + 'top 0.5s cubic-bezier(0.34, 1.56, 0.64, 1)';
    pet.style.left = `${x}px`;
    pet.style.top = `${y}px`;

    // 移动时播放移动动画
    petSpine.setAnimation('move_' + movementStyle);
    // 到达后根据情感状态播放对应动画
    petSpine.setAnimation(currentMoodAnimation);
}
```

### 11.3 漫画气泡对话框

```vue
<!-- DialogueBubble.vue -->
<template>
  <div class="bubble-container" :style="positionStyle">
    <div class="pet-response" v-html="petResponse" />
    <div class="bubble-tail" :style="tailStyle" />
  </div>
  <div class="user-input-container" :style="inputPositionStyle">
    <textarea v-model="userInput" @keydown.enter="send" />
    <button @click="send">发送</button>
  </div>
</template>

<style>
.bubble-container {
  background: white;
  border-radius: 16px;
  padding: 12px 16px;
  position: absolute;
  /* 漫画风格——稍微不规则的圆角 */
  border: 2px solid #333;
  box-shadow: 4px 4px 0 #333;
  font-size: 14px;
  line-height: 1.6;
  max-width: 280px;
}

.bubble-tail {
  /* CSS 三角形指向桌宠 */
  width: 0; height: 0;
  border-left: 12px solid transparent;
  border-right: 12px solid transparent;
  border-top: 16px solid #333;
  position: absolute;
  bottom: -16px;
}
</style>
```

---

## 十二、文件结构

```
F:/桌宠A/
├── src-tauri/                        # Rust 后端
│   ├── Cargo.toml
│   ├── src/
│   │   ├── main.rs                  # Tauri 入口
│   │   ├── commands/                # Tauri 命令
│   │   │   ├── mod.rs
│   │   │   ├── ghost_commands.rs    # Ghost 相关命令
│   │   │   ├── chat_commands.rs     # 对话相关命令
│   │   │   └── screenshot_commands.rs
│   │   ├── core/                    # Ghost 核心
│   │   │   ├── mod.rs
│   │   │   ├── ghost.rs            # Ghost 聚合根
│   │   │   ├── soul/
│   │   │   │   ├── mod.rs
│   │   │   │   ├── innate_tendency.rs
│   │   │   │   ├── sensibility.rs   # 感性（方向敏感阻尼）
│   │   │   │   ├── rationality.rs   # 理性（回弹参数）
│   │   │   │   ├── curiosity.rs
│   │   │   │   ├── impression.rs
│   │   │   │   └── soul.rs          # Soul 聚合根
│   │   │   ├── memory/
│   │   │   │   ├── mod.rs
│   │   │   │   ├── memory_base.rs
│   │   │   │   ├── short_term_memory.rs
│   │   │   │   ├── long_term_memory.rs
│   │   │   │   ├── experience.rs    # 经验系统
│   │   │   │   └── context_window.rs
│   │   │   └── prompt/
│   │   │       ├── mod.rs
│   │   │       ├── prompt_builder.rs # 结构化片段组装
│   │   │       └── token_budget.rs   # Token 预算管理
│   │   ├── services/
│   │   │   ├── mod.rs
│   │   │   ├── ai_service.rs        # 统一 AI 接入
│   │   │   ├── memory_service.rs    # 三层压缩
│   │   │   ├── screenshot_service.rs
│   │   │   ├── hotkey_service.rs
│   │   │   ├── timeline_service.rs  # 情感事件时间线
│   │   │   └── activation_service.rs
│   │   └── data/
│   │       ├── mod.rs
│   │       ├── ghost_file.rs         # ghost 文件读写
│   │       ├── database.rs           # SQLite (rusqlite)
│   │       └── encryption.rs         # AES-256-GCM 加密
│   └── migrations/
│       └── 001_initial.sql           # 数据库建表脚本
│
├── src/                              # Vue 前端
│   ├── App.vue
│   ├── main.ts
│   ├── components/
│   │   ├── PetView.vue              # 桌宠渲染容器
│   │   ├── DialogueBubble.vue       # 漫画气泡
│   │   ├── ScreenshotOverlay.vue    # 截图浮层
│   │   ├── UserInput.vue            # 用户输入框
│   │   └── SettingsPanel.vue        # 设置面板
│   ├── composables/
│   │   ├── useGhost.ts              # Ghost 状态管理
│   │   ├── useChat.ts               # 对话逻辑
│   │   ├── useAnimation.ts          # 动画状态机
│   │   └── useScreenshot.ts         # 截图交互
│   ├── styles/
│   │   ├── bubble.css               # 气泡样式
│   │   └── pet.css                   # 桌宠样式
│   └── assets/
│       └── bodies/
│           └── default/
│               ├── idle.spine        # Spine 动画（正式版）
│               └── idle/             # 帧序列（原型版）
│
├── Ghosts/                           # Ghost 文件存放
│   └── *.ghost
│
├── package.json
├── vite.config.ts
└── tauri.conf.json
```

---

## 十三、开发阶段

### 阶段一：Ghost 核心系统（2~3 周）

| 步骤 | 任务 | 产出 |
|------|------|------|
| 1.1 | 初始化 Tauri + Vue 项目 | 项目骨架 |
| 1.2 | 实现 InnateTendency（6维人格 + Beta 分布生成） | 人格生成 |
| 1.3 | 实现 Sensibility（方向敏感阻尼 + 可变基线） | 情感动力学 |
| 1.4 | 实现 EmotionalEvent + 事件系统框架 | 情感事件 |
| 1.5 | 实现 Impression（主人印象加权评分） | 印象系统 |
| 1.6 | 实现 Curiosity（好奇心 + 方向权重） | 好奇心 |
| 1.7 | 实现 Ghost 文件读写（JSON 序列化，暂不加密） | Ghost 持久化 |
| 1.8 | 集成测试：生成两个 Ghost，观察人格差异 | 验证 |

### 阶段二：记忆 + 经验系统（2 周）

| 步骤 | 任务 | 产出 |
|------|------|------|
| 2.1 | SQLite 表结构创建 + 数据库层 | 数据层 |
| 2.2 | 实现 ContextWindow（内存对话历史） | 上下文窗口 |
| 2.3 | 实现即时压缩（本地规则，零 AI 调用） | 第一层压缩 |
| 2.4 | 实现语义压缩（AI 批量总结） | 第二层压缩 |
| 2.5 | 实现经验提取（记忆→知识） | 经验系统 |
| 2.6 | 实现记忆读取逻辑（重要性 + 时间 + 随机性） | 记忆召回 |
| 2.7 | 实现 Token 预算管理 | Prompt 长度控制 |

### 阶段三：Prompt 引擎 + AI 服务（1~2 周）

| 步骤 | 任务 | 产出 |
|------|------|------|
| 3.1 | 实现结构化 Prompt 组装器 | 动态 Prompt |
| 3.2 | 实现 OpenAI 兼容 Provider | 统一 AI 接口 |
| 3.3 | 实现对话情感分析（事件提取 + 印象更新） | AI 后处理 |
| 3.4 | 端到端测试：控制台对话验证人格差异 | 验证 |

### 阶段四：桌面交互 UI（2~3 周）

| 步骤 | 任务 | 产出 |
|------|------|------|
| 4.1 | 无边框置顶透明窗口 + PetView 容器 | 桌宠窗口 |
| 4.2 | 帧序列占位渲染（色块/简单图形） | 基础桌宠 |
| 4.3 | 全局热键注册（Rust 侧） | 快捷键 |
| 4.4 | 截图覆盖层 + 选区交互 | 截图流程 |
| 4.5 | 漫画气泡对话组件 | 对话 UI |
| 4.6 | 用户输入框浮层 | 输入交互 |
| 4.7 | 桌宠移动动画（跳转到截图区域） | 移动逻辑 |
| 4.8 | 情感→动画映射 + 小动作触发器 | 视觉反馈 |

### 阶段五：可选功能（后续）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 叙事性防复制（传送仪式） | P1 | 核心叙事体验 |
| Ghost 加密 | P1 | AES-256-GCM |
| Spine 动画迁移 | P2 | 从帧序列升级 |
| TTS 语音 | P2 | edge-tts |
| 生图 API | P3 | DALL-E / Stable Diffusion |
| Enhanced 好奇心 | P3 | 后台截屏，默认关闭 |
| 情感事件时间线 UI | P3 | 可视化情感变化曲线 |

---

## 十四、决策记录

| 决策项 | v1 方案 | v2 方案 | 理由 |
|--------|---------|---------|------|
| 情感模型 | 简单弹簧拉向 0 | 方向敏感阻尼 + 可变基线 | v1 会导致长期关系总是回归平淡 |
| 防复制 | 机器指纹 DRM | 叙事性软约束 + 微扰 | DRM 伤害用户体验且易破解 |
| 记忆压缩 | 单层 AI 摘要 | 三层渐进（本地→AI→情感） | 降低 API 成本 |
| 经验系统 | 缺失 | 独立知识条目 | 设想明确提到⑤经验 |
| 技术栈 | C#/WPF/SkiaSharp | Tauri+Vue/Spine | Web 栈更适合自由形状 UI |
| Prompt | 模板库拼接 | 结构化片段+连续权重 | 避免离散跳变 |
| AI 接入 | 5 个 Provider | OpenAI 兼容协议统一 | 90%+ 模型兼容此协议 |
| 躯体 | 素材路径 | 情感→动画映射+移动风格 | 躯体是灵魂的可见形态 |
| Token 管理 | 缺失 | 明确预算分配 | 避免超限 |
| 情感事件 | 缺失 | 完整事件系统 | 没有事件就没有情感变化 |
| 主人印象 | 仅数值 | 数值+文字片段+综合好感度 | 更丰富的印象表达 |
| 人格生成 | 均匀随机 | Beta 分布 | 避免极端人格过于频繁 |

---

## 附录

### A. 与 v1 设计文档的详细对比

| 章节 | v1 | v2 | 变化 |
|------|----|----|------|
| 情感模型 | `force=(0-x)*k` | 方向敏感阻尼+基线偏移 | 根本性重写 |
| 防复制 | 机器指纹锁定 | 叙事性绑定+微扰 | 根本性重写 |
| 记忆 | 两层（STM/LTM） | 三层（STM/LTM/Experience） | 新增经验层 |
| 压缩 | AI摘要一层 | 三层渐进 | 降低成本 |
| Prompt | 模板库 | 结构化片段连续生成 | 避免跳变 |
| 体格 | 未定义 | 完整映射系统 | 新增 |
| 经验 | 缺失 | 完整系统 | 新增 |
| 事件系统 | 缺失 | 完整枚举+检测流程 | 新增 |
| Token预算 | 缺失 | 明确分配 | 新增 |

### B. 参考资料

- 攻壳机动队 Ghost 概念 → 灵魂不可复制的设计灵感
- Shimeji / Desktop Mascot → 桌宠 UI 交互参考
- Spine 2D Animation → 骨骼动画方案
- Tauri 2.x Documentation → 桌面应用框架
- OpenAI API (Chat Completions) → AI 统一接入协议参考

### C. 术语表

| 术语 | 定义 |
|------|------|
| Ghost | 灵魂数据文件，包含人格、记忆、情感等核心数据 |
| Soul | 灵魂的运行时对象，对应 Ghost.soul |
| InnateTendency | 天生倾向性，6维人格属性 |
| Sensibility | 感性系统，方向敏感阻尼模型 |
| Rationality | 理性系统，回弹参数（嵌入感性系统） |
| Baseline | 情感基线偏移，重大事件可永久改变 |
| Direction Modifier | 方向修正系数，模拟"偏见" |
| Deviation Modifier | 偏离修正，越偏离越难回弹 |
| Curiosity | 好奇心系统，主动认知世界 |
| Impression | 主人印象，加权评分+文字片段 |
| Experience | 经验，可迁移的知识技能 |
| EmotionalEvent | 情感事件，触发 LoveHate 变动的机制 |
| Soul Signature | 灵魂签名，用于叙事性防复制 |
| Generation | 传送代数，每次传送递增 |